
apiVersion: "v1"
kind: "Service"
metadata:
  name: "httpd-meta"
  labels:
    app: "httpd-meta"
spec:
  selector:
    app: "httpd-meta"
  type: "ClusterIP"
  ports:
    - port: {{$.Values.ListenPort}}



